{
  "name": "guanghui-ui",
  "version": "0.0.5",
  "private": false,
  "scripts": {
    "dev": "vite --host",
    "build": "run-p build-only",
    "build-only": "vite build",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path .gitignore"
  },
  "dependencies": {
    "vue": "^3.4.38",
    "@ghui/components": "workspace:*"
  }
}
